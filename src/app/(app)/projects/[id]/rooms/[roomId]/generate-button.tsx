'use client'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function GenerateButton(_props: { roomId: string; credits: number }) {
  return null // Placeholder — generation is handled via the /generate sub-route
}
